// 설정 템플릿 — 이 파일은 Git에 포함됩니다 (키 없음).
// 사용법: 이 파일을 js/config.js 로 복사 후 실제 값을 입력하세요.
const SUPABASE_URL = 'https://your-project-id.supabase.co';
const SUPABASE_KEY = 'your-anon-public-key'; // Supabase → Project Settings → API → anon public
